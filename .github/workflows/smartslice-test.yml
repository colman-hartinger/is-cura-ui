name: Smart Slice Tests

on:
  push:

jobs:
  build:
    container: tetonsim/smartslicetests:latest
    runs-on: ubuntu-latest
#    strategy:
#      matrix:
#        cura-version: [4.6]
    steps:
    - name: Checkout SmartSlice
      uses: actions/checkout@v2
    - name:  test 1
      run: |
        chmod +x run_in_docker.sh
        ./run_in_docker.sh